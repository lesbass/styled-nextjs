import { DefaultTheme } from 'styled-components';

const firstTheme: DefaultTheme = {
    borderRadius: '5px',

    colors: {
        main: 'cyan',
        secondary: 'magenta',
    },
};
const secondTheme: DefaultTheme = {
    borderRadius: '5px',

    colors: {
        main: 'red',
        secondary: 'blue',
    },
};

export { firstTheme, secondTheme };
